"""Split a corpus record into indexable `src.schemas.Passage` chunks.

# Phase 2 (docs/t0.md step 8): `chunk_document(corpus_id, title, text,
# settings)` — "article" strategy (keep the record as-is) and "khoan"
# strategy (split by Khoản/Điểm, doc_id always points back to the parent
# Điều). Stub only for Phase 1 — no logic yet.
"""

from __future__ import annotations
