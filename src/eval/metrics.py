"""Retrieval metrics: Recall, Precision, MRR, nDCG, MAP, hit_rate @ k.

# Phase 2 (docs/t0.md step 13): `metrics_at_k(ranked, relevance, k)` and
# `aggregate(runs, qrels, ks)` (macro-average across queries). Stub only
# for Phase 1 — no logic yet.
"""

from __future__ import annotations
